'use strict';
import * as React from 'react';
import {
  IconButton,
  // Button
} from '@mui/material';
export default function UpIcon(): JSX.Element {
  return (
    <React.StrictMode>
      <IconButton>
        △
      </IconButton>
    </React.StrictMode>
  );
}