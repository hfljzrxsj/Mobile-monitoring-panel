'use strict';
import * as React from 'react';
import { IconButton } from '@mui/material';
export default function DownIcon(): JSX.Element {
  return (
    <React.StrictMode>
      <IconButton>
        ▽
      </IconButton>
    </React.StrictMode>
  );
}